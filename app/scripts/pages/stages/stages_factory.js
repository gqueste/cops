angular.module('cops').

    factory('StagesService', [function(){

        var stagesDisponibles = 2;
        var stages = {

        };

        var stagesObtenus = [];

        return{

        };

    }]);